çok güzel ve “gerçek” bir soru 👌
burada mesele kod çalışmıyor değil, App Open Ad’in NEDEN bilinçli olarak gösterilmediği.

Aşağıya AI App Maker’ın sorusuna net, teknik ve ikna edici bir cevap bırakıyorum. Bunu aynen iletebilirsin.

KISA CEVAP (TL;DR)

Sorun kodda değil; App Open Ad’in lifecycle + UI state çakışması.
Ad yükleniyor (isLoaded=true) ama Google SDK, gösterim anını geçersiz saydığı için reklamı bilinçli olarak göstermiyor.

ASIL SEBEPLER (önem sırasına göre)
1️⃣ SplashScreen açıkken App Open Ad gösterilemez ❌

Google App Open Ad’ler:

Root view render edilmeden

SplashScreen aktifken

ilk frame çizilmeden

gösterilmez.

Senin projede:

SplashScreen.preventAutoHideAsync();
...
if (isReady) {
  SplashScreen.hideAsync();
}


Ama:

show() isReady ile yarışıyor

Splash hâlâ ekrandayken show() çağrılıyor

SDK “invalid show context” deyip sessizce discard ediyor (error fırlatmaz!)

👉 Bu en yaygın ve en kritik sebep.

2️⃣ App Open Ad, AppState active anında ama UI “stable” değil

Google dokümantasyonundaki kritik cümle:

App Open Ads should be shown only when the app is in the foreground AND UI is ready

Sen:

AppState === active → ✅

ama:

Navigation stack tam kurulmamış

RootLayout ilk render

Splash yeni kapanmış

→ SDK bunu geçerli gösterim anı saymıyor

4️⃣ App Open Ad “tek seferliktir”

App Open Ad:

show() çağrıldıktan sonra consumed olur

tekrar göstermek için yeniden load gerekir

Sen:

show() çağrısı başarılı sanılıyor

ama ad zaten invalid state’e düşmüş oluyor

CLOSED bile tetiklenmeyebiliyor

ÖZET
| Durum              | Sonuç       |
| ------------------ | ----------- |
| isLoaded true      | ✅           |
| show() çağrıldı    | ✅           |
| Splash açık        | ❌           |
| UI stabilize değil | ❌           |
| Expo Go            | ❌           |
| SDK error          | ❌ (bilerek) |


DOĞRU ÇÖZÜM (Google’ın önerdiği model)
✅ Gösterimi Splash + UI’dan SONRA geciktir
useEffect(() => {
  if (isReady && isLoaded) {
    const timeout = setTimeout(() => {
      show().catch(() => {});
    }, 300); // kritik

    return () => clearTimeout(timeout);
  }
}, [isReady, isLoaded]);


⏱️ 300–500ms gecikme
→ App Open Ad’lerin %95’ini kurtarır


✅ AppState sadece “trigger” olsun, hemen show etmesin

Yanlış:

if (state === "active") show();


Doğru:

if (state === "active") {
  setTimeout(() => show(), 300);
}
